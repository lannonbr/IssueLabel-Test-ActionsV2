name: Add Labels on Push
on: push
jobs:
  add-labels:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - uses: lannonbr/issue-label-manager-action@94d3aa42941860a0634a34956e9468e554193fa5
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

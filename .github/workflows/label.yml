name: Add Labels on Push
on: push
jobs:
  add-labels:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - uses: lannonbr/issue-label-manager-action@3d27d3f4ea6a700f2e28451d7bc52ccc815ae331
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

name: Add Labels on Push
on: push
jobs:
  add-labels:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - uses: lannonbr/issue-label-manager-action@6da4caea1561293af81bebd8138e129f90c444df
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
